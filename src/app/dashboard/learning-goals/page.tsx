import type { Metadata } from 'next';
export const metadata : Metadata = {
    title: 'My Learning Goals',
    description: 'A list of editable learning goals.',
}

export default function learningGoals() {
    return (
        <div>test</div>
    )
}